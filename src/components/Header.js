import React from 'react';

const Header = props =>
  <header role="heading">
    <h1>Flash Cards</h1>
      <nav>
        <a href="/">Cards</a>
        <a href="/logout">Logout</a>
      </nav>
  </header>

export default Header;
